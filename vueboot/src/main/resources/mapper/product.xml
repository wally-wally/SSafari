<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ssafy.product">


	<select id="getProductList"
		resultType="com.ssafy.edu.vue.dto.Product">
		SELECT * FROM PRODUCT ORDER BY PRDNO
	</select>

	<select id="getCountProduct" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM
		PRODUCT
	</select>
	<select id="findProductdetail" parameterType="java.lang.Integer"
		resultType="com.ssafy.edu.vue.dto.Product">
		SELECT * FROM PRODUCT WHERE PRDNO = #{prdno}
	</select>
	<select id="searchProductByName"
		parameterType="java.lang.String"
		resultType="com.ssafy.edu.vue.dto.Product">
		SELECT * FROM PRODUCT WHERE PRDNAME like
		concat('%',#{prdname},'%')
	</select>
	<select id="searchProductByMaterial"
		parameterType="java.lang.String"
		resultType="com.ssafy.edu.vue.dto.Product">
		SELECT * FROM PRODUCT WHERE PRDMATERIAL like
		concat('%',#{prdmaterial},'%')
	</select>
	<select id="searchProductByMaker"
		parameterType="java.lang.String"
		resultType="com.ssafy.edu.vue.dto.Product">
		SELECT * FROM PRODUCT WHERE PRDMAKER like
		concat('%',#{prdmaker},'%')
	</select>


	<insert id="saveProduct"
		parameterType="com.ssafy.edu.vue.dto.Product">
		INSERT INTO
		PRODUCT(PRDNAME, PRDMATERIAL, PRDCAPACITY, PRDCALORIE, PRDCARBO, PRDPROTEIN, PRDFAT, PRDSUGARS, PRDNATRIUM, PRDCHOLEST, PRDSFA, PRDTRANSFAT, PRDPROFIT, PRDMAKER, PRDKIND, PRDIMG )
		VALUES(#{prdname},#{prdmaterial},#{prdcapacity},#{prdcalorie},#{prdcarbo},#{prdprotein},#{prdfat},#{prdsugars},#{prdnatrium},#{prdcholest},#{prdsfa},#{prdtransfat},#{prdprofit},#{prdmaker},#{prdkind},#{prdimg})
	</insert>
	<update id="updateProduct"
		parameterType="com.ssafy.edu.vue.dto.Product">
		UPDATE PRODUCT SET
		PRDNAME = #{prdname},
		PRDMATERIAL =
		#{prdmaterial},
		PRDCAPACITY = #{prdcapacity},
		PRDCALORIE =
		#{prdcalorie},
		PRDCARBO = #{prdcarbo},
		PRDPROTEIN = #{prdprotein},
		PRDFAT = #{prdfat},
		PRDSUGARS = #{prdsugars},
		PRDNATRIUM =
		#{prdnatrium},
		PRDCHOLEST = #{prdcholest},
		PRDSFA = #{prdsfa},
		PRDTRANSFAT = #{prdtransfat},
		PRDPROFIT = #{prdprofit},
		PRDMAKER =
		#{prdmaker},
		PRDKIND = #{prdkind},
		PRDIMG = #{prdimg}
		WHERE
		PRDNO=#{prdno}
	</update>

	<delete id="deleteProduct" parameterType="java.lang.Integer">
		DELETE FROM PRODUCT
		WHERE PRDNO
		= #{prdno}
	</delete>




</mapper>
